enum MenuCardVariant {
  /// Card quadrado usado no grid 2 colunas — mesma lógica de antes,
  /// só sem a caixinha colorida atrás do ícone.
  standard,

  /// Card quadrado usado no grid 2 colunas, mas com a caixinha colorida
  /// atrás do ícone.
  featured,

  /// Card quadrado usado no grid 2 colunas, mas com o card desabilitado.
  disabled,
}